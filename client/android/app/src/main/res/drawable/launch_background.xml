<?xml version="1.0" encoding="utf-8"?>
<!-- PROMPT_CLAUDE_CODE_SPLASH_REDESIGN_V2.md (12/09/2026): antes disto
     era o branco padrão do template do Flutter, causando um flash
     branco nativo (pré-Flutter) sempre que o app abria em tema escuro
     — a maioria dos usuários, já que o app abre escuro por padrão
     (ThemeModeService._isDark = true). Cor igual a AppColors.bg claro
     (#FAF7F0) — este é o recurso `drawable/` base, usado quando o
     qualificador `night` (abaixo) não se aplica. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background_color" />
</layer-list>
